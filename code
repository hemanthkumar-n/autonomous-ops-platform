summary = {
    "pod_name": incident.pod_name,
    "namespace": incident.namespace,
    "phase": incident.phase,
    "node": incident.node,
    "metrics": (
        incident.metrics.model_dump()
        if incident.metrics
        else {}
    ),
    "events": incident.events,
    "containers": [
        container.model_dump()
        for container in incident.container_states
    ],
}